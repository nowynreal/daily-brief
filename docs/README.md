# Daily Brief Site

This folder is intended for static publishing (for example GitHub Pages).

- `index.html` is the archive page.
- `briefs/YYYY-MM-DD.html` files are generated daily.

Do not store secrets in this folder.
